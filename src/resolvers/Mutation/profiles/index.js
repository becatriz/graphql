const { createProfile } = require("./create");
const { removeProfile } = require("./remove.js");
const { updateProfile } = require("./update");

module.exports = {
  createProfile,
  removeProfile,
  updateProfile,
};
