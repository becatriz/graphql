const { findIndex } = require("./findIndex");
const {
  validatesExistingProfile,
  validatesExistingEmail,
} = require("./validate");

module.exports = {
  findIndex,
  validatesExistingProfile,
  validatesExistingEmail,
};
